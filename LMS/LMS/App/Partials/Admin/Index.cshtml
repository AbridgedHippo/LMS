
@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated && User.IsInRole("Admin"))
{
    <h3>Admin Index</h3>
    <p>
        Nothing here yet...
    </p>
}
else
{
    Response.Redirect("~/");
}
